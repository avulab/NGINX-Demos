version: 2.1
jobs:
  Infra:
    docker:
    - image: bitnami/kubectl  

    steps:
      - run:
          name: Build Infra
          command: |
            echo make infra
            eksctl create cluster --name nginxhellosimple --region us-west-2 --nodegroup-name nginx-nodes --node-type t2.small --nodes 3 --nodes-min 1 --ssh-access --ssh-public-key udacity
            kubectl create namespace nginxhellosimple
    
  Lint:
    machine: true  
    working_directory: ~/NGINX-Demos/nginx-hello

    steps:
      - checkout:
          path: ~/NGINX-Demos

      - run:
          name: install dependencies
          command: |
            make lintinstall

      - run:
          name: run lint
          command: |
            make lint 

  Build_and_Upload:
    machine: true  
    working_directory: ~/NGINX-Demos/nginx-hello

    steps:
      - checkout:
          path: ~/NGINX-Demos

      - run:
          name: build image
          command: |
            make build 

      - run:
          name: upload image
          command: |
            make login tag upload 

  Deploy_and_Run:
    docker: 
      - image: amazon/aws-cli
    working_directory: ~/NGINX-Demos/nginx-hello

    steps:
      - checkout:
          path: ~/NGINX-Demos
      - run:
          name: Config kubectl
          command: |
            aws eks --region us-west-2  update-kubeconfig --name nginxhellosimple
      - run:
          name: rolling update
          command: |
            make deploy

workflows:
  version: 2
  lint-build-and-deploy:
    jobs:
      - Infra
      - Lint
      - Build_and_Upload:
          requires:
            - Lint
      - Deploy_and_Run:
          requires:
            - Build_and_Upload